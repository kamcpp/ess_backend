table! {
    employee (id) {
        id -> Int4,
        employee_nr -> Varchar,
        first_name -> Varchar,
        second_name -> Varchar,
        username -> Varchar,
        office_email -> Varchar,
        mobile -> Varchar,
    }
}
